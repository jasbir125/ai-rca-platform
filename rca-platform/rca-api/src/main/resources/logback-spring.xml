<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="serviceName" value="rca-api"/>
    <property name="environment" value="${APP_ENVIRONMENT:-local}"/>
    <property name="version" value="${APP_VERSION:-1.0.0}"/>

    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service":"${serviceName}","environment":"${environment}","version":"${version}"}</customFields>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <logger>logger</logger>
                <thread>thread</thread>
                <level>level</level>
            </fieldNames>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="JSON"/>
    </root>
</configuration>
